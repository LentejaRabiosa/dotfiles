-- local ok, _ = pcall(vim.cmd, "colorscheme xcodewwdc")
local ok, _ = pcall(vim.cmd, "colorscheme onedark")
-- local ok, _ = pcall(vim.cmd, "colorscheme rose-pine")
-- local ok, _ = pcall(vim.cmd, "colorscheme everforest")
