// -*- mode: jsonc -*-
{
    "layer": "top", // Waybar at top layer
    "spacing": 0, // Gaps between modules (4px)
    "reload_style_on_change": true,

    // Choose the order of the modules
    "modules-left": [
        "hyprland/workspaces"
    ],

    "modules-center": [
    ],

    "modules-right": [
        "load",
        "cpu",
        "memory",
        "temperature",
        "backlight",
        "battery",
        "wireplumber",
        "hyprland/language",
        "user",
        "clock"
    ],

    // Modules configuration

    "hyprland/workspaces": {
        "disable-click": true,
        "disable-scroll": true,
        "on-click": "",
        "on-scroll-up": "",
        "on-scroll-down": "",
        "format": "{icon}",
        "format-icons": {
            "1": "I",
            "2": "II",
            "3": "III",
            "4": "IV",
            "5": "V",
            "6": "VI",
            "7": "VII",
            "8": "VIII",
            "9": "IX",
            "10": "X"
        }
    },

    "clock": {
        "format-alt": "{:%Y-%m-%d}",
        "tooltip": false
    },

    "user": {
        "format": "{work_H} h {work_M} min",
        "tooltip": false
    },

    "hyprland/language": {
        "format": "{}",
        "format-es": "es",
        "format-en": "en"
    },

    "load": {
        "format": "loa {load1}",
        "format-alt": "loa {load1} {load5} {load15}",
        "tooltip": false
    },

    "cpu": {
        "format": "cpu {usage}%",
        "tooltip": false
    },

    "memory": {
        "format": "mem {}%",
        "format-alt": "mem {used}/{total}GiB",
        "tooltip": false
    },

    "temperature": {
        "format": "tem {temperatureC}°C",
        "tooltip": false
    },

    "backlight": {
        "format": "bri {percent}%",
        "scroll-step": 5.0,
        "smooth-scrolling-threshold": 5.0,
        "tooltip": false
    },

    "battery": {
        "format": "bat {capacity}%",
        "format-charging": "bat cha {capacity}%",
        "format-plugged": "bat plu {capacity}%",
        "format-alt": "bat {time}",
        "tooltip": false
    },

    "wireplumber": {
        "format": "vol {volume}%",
        "format-muted": "mut",
        "scroll-step": 5.0,
        "smooth-scrolling-threshold": 5.0,
        "tooltip": false,
        "on-click": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"
    }
}
